name: app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.19.3
  google_fonts: ^2.0.0
  cupertino_icons: ^1.0.0
  http: ^0.13.1
  provider: ^5.0.0
  audioplayers: ^0.18.3
  fluttertoast: ^8.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
